# Copy to .env.admin.local only when an administrator needs to reset the cloud.
# Never prefix this key with VITE_ and never ship the file with the app.
SUPABASE_URL=https://PROJECT_REF.supabase.co
SUPABASE_SECRET_KEY=sb_secret_...
